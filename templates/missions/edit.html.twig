{% extends 'base.html.twig' %}

{% block title %}Editer la Mission{% endblock %}

{% block body %}
    <h1>Editer la Mission</h1>

    {{ include('missions/_form.html.twig', {'button_label': 'Update'}) }}
    {{ include('missions/_delete_form.html.twig') }}

    <a href="{{ path('missions_index') }}">retour a la liste</a>
{% endblock %}
